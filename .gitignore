__pycache__/
*.py[cod]
*$py.class
*.so
.env
.venv/
venv/
*.egg-info/
dist/
build/
.DS_Store
.mcp.json
transcripts/
audio_cache/
tmp_audio/
.episode_catalog.json

# Local AI-assistant tooling / notes (not part of the product)
CLAUDE.md
CLAUDE.local.md
HANDOFF.local.md
.claude/

# LinkedIn project assets (writeups, screenshots, GIFs — local-only)
linkedin/

# Slide-deck source — lives in this repo's `gh-pages` branch (the published deck),
# kept here only as a local working copy. Not part of the app on `main`.
podsearch-deck/

# Scratch / local-only working files
tmp/

# Server-only deploy override (shared-droplet specifics)
docker-compose.override.yml
