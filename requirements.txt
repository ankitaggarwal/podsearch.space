# Core
psycopg2-binary>=2.9.10   # PostgreSQL: episode catalog + raw transcripts
openai>=2.0.0             # OpenAI-compatible SDK — answer synthesis (e.g. Google AI Studio)
feedparser>=6.0.0
requests>=2.33.0          # Pinecone REST (integrated embedding) + transcription gateway
python-dotenv>=1.0.0      # loads .env for local development

# Web server
fastapi>=0.130.0
uvicorn>=0.46.0
mcp>=1.27.0
